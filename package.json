{
  "name": "form-training-nutrition",
  "version": "3.0.0",
  "private": true,
  "description": "Form — Training & Nutrition (Capacitor Android build)",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
